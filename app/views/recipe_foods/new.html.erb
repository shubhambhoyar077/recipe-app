<%= form_with model:([@recipe, @recipe_food]) do |form|%>
<h1>Add Food in Recipe</h1>

<div>
  <%= form.label :food_id %>
  <%= form.collection_select :food_id, Food.all, :id, :name %>
</div>

<%=form.label :quantity%>
<%=form.number_field :quantity%>

<%=form.submit "Create"%>
<%end%>